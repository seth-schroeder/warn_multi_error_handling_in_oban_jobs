defmodule WarnMultiErrorHandlingInObanJobsTest do
  use ExUnit.Case
  doctest WarnMultiErrorHandlingInObanJobs

  test "greets the world" do
    assert WarnMultiErrorHandlingInObanJobs.hello() == :world
  end
end
